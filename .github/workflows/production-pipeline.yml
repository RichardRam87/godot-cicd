name: 🚀 Production Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    name: 🎲 Release Build
    uses: ./.github/workflows/test-and-build.yml
    with:
      build-type: 'release'

